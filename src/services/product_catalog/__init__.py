"""Product catalog and availability (inventory) services.

This module is intentionally lightweight:
- Works without external vector DB (keyword fallback)
- Can be extended to use Milvus for RAG-style semantic retrieval
"""

